{
  "name": "components",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "build-class": "REACT_APP_HOOKS=0 react-scripts build",
    "build-hooks": "REACT_APP_HOOKS=1 react-scripts build",
    "build-hooks-optimized": "REACT_APP_HOOKS=2 react-scripts build",
    "serve": "serve -s build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "serve": "^10.1.2"
  }
}
